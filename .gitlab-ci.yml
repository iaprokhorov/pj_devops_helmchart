build_image:
  image: docker:git
  services:
  - docker:dind
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t registry.gitlab.com/pj-devops/django-app .
    - docker push registry.gitlab.com/pj-devops/django-app
  only:
    - master